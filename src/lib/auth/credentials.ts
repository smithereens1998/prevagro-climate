/**
 * Credenciais do usuário padrão criado por `backend/scripts/seed_default_user.py`.
 * Substituir por `POST /auth/login` quando o backend expuser autenticação.
 */
export const SEED_USER_EMAIL = "prevagro@gmail.com";

/** Mesmo valor do script de seed — apenas para validação local até existir API de auth. */
export const SEED_USER_PASSWORD = "@admin1";
