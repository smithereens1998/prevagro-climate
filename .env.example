# Mapbox (obrigatório para mapas)
VITE_MAPBOX_TOKEN=

# Backend Prevagro — em dev use /api com proxy do Vite (vite.config.ts)
# Em produção: URL completa, ex. https://api.seudominio.com
VITE_API_BASE_URL=/api
